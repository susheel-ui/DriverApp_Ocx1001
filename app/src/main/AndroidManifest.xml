<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 🌐 Core -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- 📍 LOCATION -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- 🔔 Notifications -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- 🔊 Wake + vibration -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- 🔥 Foreground services -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_PHONE_CALL" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <!-- 📷 Camera -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!-- ✅ REQUIRED FOR ANDROID 11+ (MUST BE HERE) -->
    <queries>
        <package android:name="com.google.android.apps.maps" />
    </queries>

    <application
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OCX_1001_DriverApp">

        <!-- 🗺 GOOGLE MAPS API KEY -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBO2XTkgKOQ0ItAjGyxi_g406jnvFeg4zo" />

        <!-- 🔥 Razorpay -->
        <meta-data
            android:name="com.razorpay.ApiKey"
            android:value="rzp_live_S0hfvT0Yw14rlq"/>

        <!-- 🔥 Foreground Services -->
        <service
            android:name=".RideForegroundService"
            android:exported="false"
            android:foregroundServiceType="phoneCall" />

        <service
            android:name=".DriverLocationService"
            android:exported="false"
            android:foregroundServiceType="location" />

        <!-- 🔥 Firebase Messaging -->
        <service
            android:name=".DriverFirebaseService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- 🚀 Launcher -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 🔥 FULL SCREEN POPUP -->
        <activity
            android:name=".RideRequestPopupActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:excludeFromRecents="true"
            android:theme="@style/Theme.DriverApp.FullScreenPopup" />

        <!-- 🚕 LIVE RIDE -->
        <activity
            android:name=".LiveRideActivity"
            android:exported="false"
            android:launchMode="singleTop" />

        <!-- Other screens -->
        <activity android:name=".LoginActivity" android:exported="true" />
        <activity android:name=".RegistrationActivity" android:exported="true" />
        <activity android:name=".ResistrationDriver" android:exported="true" />
        <activity android:name=".Otp_VerificationActivity" android:exported="false" />
        <activity android:name=".DashboardActivity" android:exported="true" />
        <activity android:name=".AllTripsActivity" android:exported="true" />
        <activity android:name=".CollectPaymentActivity" android:exported="true" />
        <activity android:name=".SupportActivity" android:exported="true" />
        <activity android:name=".UserProfileActivity" android:exported="true" />
        <activity android:name=".QrPaymentActivity" android:exported="true" />
        <activity android:name=".WithdrawRequestsActivity" android:exported="true" />

        <!-- 📁 File provider -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

    </application>

</manifest>
